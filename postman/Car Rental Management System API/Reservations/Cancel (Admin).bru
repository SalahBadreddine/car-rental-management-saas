meta {
  name: Cancel (Admin)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/reservations/RESERVATION_ID_HERE
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
}
