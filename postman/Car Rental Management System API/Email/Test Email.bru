meta {
  name: Test Email
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/email/test
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "to": "your-email@example.com"
  }
}
