meta {
  name: Delete Car (Admin)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/cars/CAR_ID_HERE
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
}
