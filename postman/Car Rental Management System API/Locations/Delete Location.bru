meta {
  name: Delete Location
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/locations/:id
  body: none
  auth: inherit
}

params:path {
  id: 
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
