meta {
  name: Get Reservation Payments
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/payments/reservation/:reservationId
  body: none
  auth: bearer
}

params:path {
  reservationId: your-reservation-uuid-here
}

auth:bearer {
  token: {{accessToken}}
}
