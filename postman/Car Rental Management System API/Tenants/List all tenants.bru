meta {
  name: List all tenants
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/tenants
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
