meta {
  name: Verify Email
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/auth/verify-email
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "VERIFICATION_TOKEN_FROM_EMAIL"
  }
}

settings {
  encodeUrl: true
}
